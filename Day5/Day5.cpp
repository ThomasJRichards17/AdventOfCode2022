#include <iostream>

/*
    Paraphrased Description:
      - Supplies stored in stacks of marked crates
      - Crates can be moved from one stack to another
      - Moves specified on each line (move X from Y to Z)
        - Crates moved one at a time
          - If X = 3, [1,2,3] ends up like [3,2,1]

    Goal:
     - Find which crate ends up on top of each stack
*/

int main()
{
    std::cout << "Hello World 2!\n";
}